import { Slice } from "@/world/slice";

/**
 * The Three.js scroll sequence built in this repo.
 *
 * Kept reachable while `/` serves the downloaded artifact build, so neither
 * implementation is lost while you decide which one the portfolio ships.
 */
export default function WorldPage() {
  return <Slice />;
}
